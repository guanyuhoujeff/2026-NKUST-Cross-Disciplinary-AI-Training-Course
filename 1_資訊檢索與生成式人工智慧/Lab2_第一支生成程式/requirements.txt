# Lab 2：第一支生成程式
# 安裝：pip install -r requirements.txt

# ── Lab 2 今天要用的 ──
langchain-ollama        # ChatOllama：呼叫 Ollama Cloud 生成
requests                # 環境自我檢查（打 localhost:11434 看服務通不通）
jupyter                 # notebook 本體

# ── Lab 3 才會用到，趁現在一起裝好（下午不用再等）──
langchain-community     # FAISS 向量庫的 LangChain 包裝
langchain-text-splitters # 把長文件切塊
faiss-cpu               # 向量庫本體
# ⚠️ Windows 上 faiss-cpu 偶爾裝不起來 → Lab 2 不受影響，Lab 3 前再處理

# ── D 段（加映・選配）：商用 API 對照 ──
# 只有想跑「Gemini vs 開源模型」那段才需要；不裝也不影響 Lab 2 / Lab 3
langchain-google-genai  # ChatGoogleGenerativeAI：呼叫 Gemini（key 到 https://aistudio.google.com/apikey 免費辦）
